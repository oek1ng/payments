[project]
name = "consumer"
version = "0.1.0"
description = "Payment processing consumer"
requires-python = ">=3.14"
dependencies = [
    "faststream[rabbit]>=0.7.1",
    "aiosqlite>=0.20.0",
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
